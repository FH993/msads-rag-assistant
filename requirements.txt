# Pinned to the exact versions verified working locally, so Streamlit Cloud
# installs the same compatible set (avoids the chromadb/protobuf import crash).
streamlit==1.54.0
langchain-core==1.2.28
langchain-openai==1.1.12
langchain-chroma==1.1.0
langchain-text-splitters==1.1.1
chromadb==1.5.7
openai==2.31.0
tiktoken==0.12.0
beautifulsoup4==4.14.3
requests>=2.31
datasets==4.8.4

# Pin the protobuf / opentelemetry chain that chromadb pulls in — this is the
# combination that crashed on the cloud when left unpinned.
protobuf==6.33.5
opentelemetry-api==1.41.0
opentelemetry-sdk==1.41.0
opentelemetry-exporter-otlp-proto-grpc==1.41.0
